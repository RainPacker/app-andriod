<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--标题栏（actionBar）颜色-->  
        <item name="colorPrimary">@color/title_bg</item>
        <!--状态栏颜色-->  
        <item name="colorPrimaryDark">@color/title_bg</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/act_bg</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="base_dialog_style" parent="@android:style/Animation.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="base_bottom_dialog_anim_style" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/base_bottom_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/base_bottom_dialog_exit</item>
    </style>


    <!--自定义通知内容颜色-->
    <style name="NotificationInfo"
           parent="android:TextAppearance.StatusBar.EventContent"/>

    <!--自定义通知标题颜色-->
    <style name="NotificationTitle"
           parent="android:TextAppearance.StatusBar.EventContent.Title"/>

    <!--透明Activity-->
    <style name="translucentActivity" parent="AppTheme">
        <!--<item name="android:windowIsFloating">true</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
